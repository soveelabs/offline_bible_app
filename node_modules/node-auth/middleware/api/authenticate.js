exports = module.exports = function(req, res, next) {
  if (req.auth.error) { return res.status(500).json(req.auth.error) };

  if (req.auth.user.error) {
    return res.status(req.auth.response.statusCode).json(req.auth.user);
  } else {
    return next();
  }
}
