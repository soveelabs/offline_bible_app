var url = require('url'),
    config = require('../../config'),

exports = module.exports = function (req, res, next) {
  var pathName = url.parse(req.originalUrl).pathname;

  if (config.unprotectedUrls.indexOf(pathName) >= 0) {
    return next('route');
  } else {
    return next();
  }
}
