var Requests = require('../../lib/requests');

exports = module.exports = function (req, res, next) {
  Requests.validateToken(req.auth.token, function(err, response, user) {
    req.auth.error = err;
    req.auth.response = response;
    req.auth.user = user;

    return next();
  });
}
